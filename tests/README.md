## Run unittests

Go to the root folder and run:

`python -m unittest tests.{test_name}`

where `{test_name}` is the name of the concrete test. Some examples:

- `python -m unittest tests.preprocessor`
- `python -m unittest tests.predictor_tests.automute`